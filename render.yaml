services:
  - type: web
    name: milano-quartieri
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_nil:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
